{
  "entities": {
    "Lead": {
      "title": "Lead",
      "description": "Information provided by a potential customer interested in the book.",
      "type": "object",
      "properties": {
        "fullName": {
          "type": "string",
          "description": "The full name of the person."
        },
        "email": {
          "type": "string",
          "format": "email",
          "description": "The email address of the person."
        },
        "whatsapp": {
          "type": "string",
          "description": "The WhatsApp number of the person."
        },
        "createdAt": {
          "type": "string",
          "format": "date-time",
          "description": "When the lead was created."
        }
      },
      "required": ["fullName", "email", "whatsapp", "createdAt"]
    }
  },
  "firestore": {
    "/leads/{leadId}": {
      "schema": "Lead",
      "description": "Collection of leads from the interest form."
    }
  }
}
